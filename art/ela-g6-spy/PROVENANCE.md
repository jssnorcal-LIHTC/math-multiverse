# Cold Signal (G6, English): Provenance

Every figure in this pack is an ORIGINAL BUILT GRAPHIC. Nothing here is photographed, scanned,
traced or sourced from a third party, so no licence question arises and none of the sourcing
rules that govern the science and history packs apply.

What DOES govern this pack is transcription. Each figure is generated deterministically from a
`dataTable` committed inside `packs/ela-g6-spy.json`, and every label in that table is a
verbatim substring of the passage the figure illustrates. That is not a convention: it is
enforced by `tests/figure-fidelity.js`, which fails the build for any label it cannot find in
the named passage, for any figure whose committed drawing omits a label its table declares, and
for any caption that states a number or an exclusivity the passage does not.

`tests/figure-derive.js` separately regenerates every SVG below from its own `dataTable` and
byte-compares it against the committed file, so a drawing and the data behind it cannot drift
apart unnoticed.

## Built assets (originally authored for this project, not sourced)

| Figure ID | File | Institution | Accession | Licence Basis | Source URL | Built | Evidence Note |
|---|---|---|---|---|---|---|---|
| fig-l1-long-way | `fig-l1-long-way.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `route`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l1-long-way.svg`. Every label is transcribed verbatim from passage `p-long-way` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l1-dead-drop | `fig-l1-dead-drop.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `schematic`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l1-dead-drop.svg`. Every label is transcribed verbatim from passage `p-dead-drop` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l2-dead-air | `fig-l2-dead-air.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `schematic`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l2-dead-air.svg`. Every label is transcribed verbatim from passage `p-dead-air` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l3-relay-room | `fig-l3-relay-room.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `timeline`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l3-relay-room.svg`. Every label is transcribed verbatim from passage `p-relay-room` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l3-the-log | `fig-l3-the-log.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l3-the-log.svg`. Every label is transcribed verbatim from passage `p-the-log` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l3-on-the-line | `fig-l3-on-the-line.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `timeline`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l3-on-the-line.svg`. Every label is transcribed verbatim from passage `p-on-the-line` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-beacon-brief | `fig-l4-beacon-brief.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l4-beacon-brief.svg`. Every label is transcribed verbatim from passage `p-beacon-brief` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-beacon-log | `fig-l4-beacon-log.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l4-beacon-log.svg`. Every label is transcribed verbatim from passage `p-beacon-log` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-petrel-brief | `fig-l4-petrel-brief.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l4-petrel-brief.svg`. Every label is transcribed verbatim from passage `p-petrel-brief` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-cold-weather-log | `fig-l4-cold-weather-log.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l4-cold-weather-log.svg`. Every label is transcribed verbatim from passage `p-cold-weather-log` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l5-three-tunnels | `fig-l5-three-tunnels.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l5-three-tunnels.svg`. Every label is transcribed verbatim from passage `p-three-tunnels` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l5-three-climates | `fig-l5-three-climates.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-04 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/ela-g6-spy/fig-l5-three-climates.svg`. Every label is transcribed verbatim from passage `p-three-climates` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
