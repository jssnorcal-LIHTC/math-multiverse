# Night Rounds (G6, English): Provenance

Every figure in this pack is an ORIGINAL BUILT GRAPHIC. Nothing here is photographed, scanned,
traced or sourced from a third party, so no licence question arises and none of the sourcing
rules that govern the science and history packs apply.

What DOES govern this pack is transcription. Each figure is generated deterministically from a
`dataTable` committed inside `packs/night-rounds-g6.json`, and every label in that table is a
verbatim substring of the passage the figure illustrates. That is not a convention: it is
enforced by `tests/figure-fidelity.js`, which fails the build for any label it cannot find in
the named passage, for any figure whose committed drawing omits a label its table declares, and
for any caption that states a number or an exclusivity the passage does not.

`tests/figure-derive.js` separately regenerates every SVG below from its own `dataTable` and
byte-compares it against the committed file, so a drawing and the data behind it cannot drift
apart unnoticed.

## Built assets (originally authored for this project, not sourced)

| Figure ID | File | Institution | Accession | Licence Basis | Source URL | Built | Evidence Note |
|---|---|---|---|---|---|---|---|
| fig-l1-first-rounds | `fig-l1-first-rounds.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `route`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l1-first-rounds.svg`. Every label is transcribed verbatim from passage `p-l1-first-rounds` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l1-tortoise-yard | `fig-l1-tortoise-yard.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `schematic`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l1-tortoise-yard.svg`. Every label is transcribed verbatim from passage `p-l1-count-that-would-not-close` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l2-diet-card | `fig-l2-diet-card.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l2-diet-card.svg`. Every label is transcribed verbatim from passage `p-l2-otter-diet-card` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l2-workup | `fig-l2-workup.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `schematic`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l2-workup.svg`. Every label is transcribed verbatim from passage `p-l2-lethargy-workup-rana` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l2-cold-chain | `fig-l2-cold-chain.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l2-cold-chain.svg`. Every label is transcribed verbatim from passage `p-l2-cold-chain-invoice` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l3-three-accounts | `fig-l3-three-accounts.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l3-three-accounts.svg`. Every label is transcribed verbatim from passage `p-l3-three-accounts` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l3-monitoring | `fig-l3-monitoring.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `timeline`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l3-monitoring.svg`. Every label is transcribed verbatim from passage `p-l3-monitoring-summary` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-fence-nine | `fig-l4-fence-nine.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `schematic`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l4-fence-nine.svg`. Every label is transcribed verbatim from passage `p-l4-maintenance-file-fence-nine` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-bulletin | `fig-l4-bulletin.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `bar`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l4-bulletin.svg`. Every label is transcribed verbatim from passage `p-l4-bulletin-fourteen` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l4-storm-clipping | `fig-l4-storm-clipping.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l4-storm-clipping.svg`. Every label is transcribed verbatim from passage `p-l4-storm-rips-fence` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l5-transfer-slip | `fig-l5-transfer-slip.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `facsimile`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l5-transfer-slip.svg`. Every label is transcribed verbatim from passage `p-l5-rehabbers-van` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l6-night-of-the-storm | `fig-l6-night-of-the-storm.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `timeline`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l6-night-of-the-storm.svg`. Every label is transcribed verbatim from passage `p-l6-night-of-the-storm` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
| fig-l6-morning-report | `fig-l6-morning-report.svg` | Original work for this project | N/A | Original work; derives from nothing else under `art/` | N/A | 2026-09-02 | Deterministic render of this figure's own `dataTable` (type `schematic`), generated by `build/figure-gen.js` into `art/night-rounds-g6/fig-l6-morning-report.svg`. Every label is transcribed verbatim from passage `p-l6-morning-report` in this pack; no photograph, scan, trace or third-party source of any kind, and no figurative content (constraint 8). Proven by `tests/figure-fidelity.js` in both directions: every table string is present in the passage, and every table string is drawn in the committed SVG. |
